using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class MaterialSlot : MonoBehaviour
{
    [Header("References")]
    [SerializeField] private UnityEngine.UI.Image materialIcon;
    [SerializeField] private TextMeshProUGUI materialName;

    //public void SetMaterial(CraftingRecipe recipe)
    //{
        
    //}
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
